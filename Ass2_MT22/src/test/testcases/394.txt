sumArray:function integer(arr: array[3] of integer, n: integer) {
            sum = 0;
                for (i = 0, i < n,i + 1) {
            sum = sum + arr[i];
                }
                return sum;
        }